_schema-version: "3.2"
ID: GestionRrhh
version: 0.0.1
modules:
- name: gestionrrhh-approuter
  type: approuter.nodejs
  path: gestionrrhh-approuter
  requires:
  - name: GestionRrhh_html_repo_runtime
  - name: uaa_GestionRrhh
  - name: GestionRrhh-dest-srv
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: GestionRrhh_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: GestionRrhh_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - logaligroupemployee.zip
      name: logaligroupemployee
      target-path: resources/
- name: logaligroupemployee
  type: html5
  path: employee
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: GestionRrhh_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_GestionRrhh
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: GestionRrhh-xsuaa-service
    service-plan: application
- name: GestionRrhh_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: GestionRrhh-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: GestionRrhh-dest-srv
    service-plan: lite
parameters:
  deploy_mode: html5-repo
